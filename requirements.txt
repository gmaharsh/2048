# Core dependencies for the SeqMixer Pareto study.
# Install torch separately for your platform, e.g. CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
numpy>=1.24
matplotlib>=3.7

# Optional (GPU only): fast Mamba CUDA kernels. The repo ships a pure-PyTorch
# reference Mamba that runs anywhere; install this only to accelerate on CUDA.
# mamba-ssm>=2.0
# causal-conv1d>=1.2
